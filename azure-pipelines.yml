trigger:
  batch: true
  branches:
    include:
      - master
      - refs/tags/v*

pr:
  - master

variables:
  PYTHONUNBUFFERED: 1
  YARN_CACHE_FOLDER: $(Pipeline.Workspace)/.yarn
  JLSP_VERSION: 0.5.0

jobs:
  - template: ci/job.test.yml
  - template: ci/job.combine.yml
